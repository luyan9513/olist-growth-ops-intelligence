name: olist_growth_ops
version: 1.0.0
config-version: 2

profile: olist_growth_ops

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

vars:
  raw_data_dir: "data/raw"

models:
  olist_growth_ops:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
