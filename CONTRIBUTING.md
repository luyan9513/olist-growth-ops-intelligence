# 贡献指南

本项目首先保证口径可信、时间顺序正确和结论可复现，再考虑增加功能。

## 开发流程

1. 阅读 `docs/01_requirements_and_metrics.md`、`docs/03_data_design.md` 和相关阶段日志。
2. 只修改当前业务问题涉及的文件；指标口径变化必须同步更新指标字典、测试和追溯矩阵。
3. 不提交 `data/raw/`、`data/processed/`、`artifacts/`、DuckDB、模型文件或本地密钥。
4. 提交前运行：

```bash
make repo-check
make dbt-parse
make test
```

涉及数据转换、训练或报告更新时，还需按 README 的完整流程重新生成产物。CI 只验证无需私有或大体积数据的部分，不能替代本地全量验证。

## 分析与模型约束

- 线索成交预测不得使用成交后字段。
- 订单创建时低评分风险预测不得使用最终评价和最终履约结果。
- 所有模型结果都要标明时间切分、基线、样本量和限制。
- 历史回测、名单覆盖和情景输入不能写成线上增量、真实 ROI 或业务收益。
- 新指标必须写清分子、分母、过滤条件、时间字段和空值处理。

## 变更说明

Pull Request 至少说明业务问题、受影响口径、验证命令、结果和剩余风险。若没有遇到阻塞问题，应在阶段日志中明确记录“本阶段未遇到阻塞问题”。
